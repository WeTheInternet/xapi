package net.wti.dsl.parser;

///
/// DslGraphGenerator:
///
/// Generates a graph factory and element classes from a DslModel.
///
/// Created by James X. Nelson (James@WeTheInter.net) on 16/12/2025 @ 01:17
public class DslGraphGenerator {
//
//    public void generate(DslModel model, Path outputDir) {
//        String dslName = model.getName();
//        String pkg = computeGraphPackage(model); // e.g. model.getPackageName() + ".graph"
//        String factoryName = dslName + "GraphFactory";
//
//        // 1) Analyze the model/root AST to discover element types and fields.
//        GraphSchema schema = analyzeModel(model);
//
//        // 2) Write <DslName>GraphFactory.java
//        writeFactory(pkg, factoryName, schema, outputDir);
//
//        // 3) Write each *El and Builder
//        for (ElementType type : schema.getElementTypes()) {
//            writeElementType(pkg, type, outputDir);
//        }
//    }
//
//    private GraphSchema analyzeModel(DslModel model) {
//        // Walk model.getRoot(), collect tags, attributes, relationships.
//        // Produce a small meta-model (GraphSchema) for easier codegen.
//    }

}
