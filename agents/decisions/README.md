# Decisions

Decision notes record owner-confirmed choices that future work should treat as settled until new evidence justifies reopening them.

Use `_template.md`. Knowledge discovered directly from code belongs in `../knowledge/`; unresolved work belongs in `../tasks/`.
