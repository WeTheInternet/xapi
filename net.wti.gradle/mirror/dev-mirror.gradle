plugins {
    id 'java'
}
apply from: "$rootDir.parent/gradle/xapi-env.gradle"

repositories {
    jcenter()
}

dependencies {
    compile "net.wti.core:xapi-gen:$version",
        "net.wti.core:xapi-fu:$version", // blech... transitive dependencies not working right.  This should not be necessary
        "net.wti.core:xapi-anno:$version"
}
