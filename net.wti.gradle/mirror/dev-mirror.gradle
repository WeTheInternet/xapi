plugins {
    id 'java-library'
}
apply from: "$rootDir.parent/gradle/xapi-env.gradle"

repositories {
    jcenter()
}

dependencies {
    api "net.wti.core:xapi-gen:$version",
        "net.wti.core:xapi-annotations:$version"
}
