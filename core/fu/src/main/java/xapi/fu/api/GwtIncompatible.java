package xapi.fu.api;

/**
 * Here so we can mark things to ignore w/out depending on GWT to actually be on the classpath.
 *
 * Created by James X. Nelson (james @wetheinter.net) on 10/29/16.
 */
public @interface GwtIncompatible {
}
