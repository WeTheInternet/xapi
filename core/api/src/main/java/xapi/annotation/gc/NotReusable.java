package xapi.annotation.gc;

/**
 * Implement this to denote a non-reusable container,
 * for example, an Iterable that just wraps an Iterable, which cannot be un-iterated..
 *
 * @author James X. Nelson (james@wetheinter.net)
 *         Created on 2/6/16.
 */
public interface NotReusable {
}
