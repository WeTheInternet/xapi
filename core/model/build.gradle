buildscript({
    dependencies {
        classpath 'net.wti.gradle.tools:xapi-gradle-tools:0.5.1'
    }
})
plugins {
    id 'xapi-require'
}

xapiRequire.module('main').configure {
    req ->
        req.project 'xapi-inject'
        req.project 'xapi-collect'
}

dependencies {
    compileOnly 'net.wetheinter:jsinterop-annotations:2.8.0'
    compile project(':xapi-dev-parent:xapi-dev-source')
    compile project(':xapi-dev-parent:xapi-dev-bytecode')
}

description = 'XApi - Data model core API'
