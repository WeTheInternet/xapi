/**
 * The set of interfaces that you need to implement
 * in order to "provide" xapi.scope.api;
 *
 * If you are using a "native" container,
 * like j2ee, vert.x, guice, dagger, etc,
 * you will likely just want to support these types
 * by wrapping some type we don't know about.
 *
 * If you want to build your own container,
 * then implement and use these interfaces,
 * and feel free to hack 'n send patches!
 *
 */
package xapi.scope.spi;
