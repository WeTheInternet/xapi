import org.gradle.api.tasks.SourceSet;

plugins {
    id	"java-library"// GenInclude net.wti.gradle.settings.plugin.XapiSettingsPlugin adding java-library b/c api dependencies used
}
// GenStart net.wti.gradle.settings.plugin.XapiSettingsPlugin
ext.xapiModern = 'true'

apply from: "$rootDir/gradle/xapi-modern.gradle"
String repo = project.findProperty("xapi.mvn.repo")
if (repo) {
    repositories {
        maven {
            name = 'xapiLocal'
            url = repo
        }
    }
}
plugins.apply 'java-library'
java.toolchain.languageVersion = JavaLanguageVersion.of(8)
repositories.mavenCentral()

SourceSet main = sourceSets.maybeCreate('main');


SourceSet test = sourceSets.maybeCreate('test');

main.java.setSrcDirs([]);
main.resources.setSrcDirs([]);
test.java.setSrcDirs([]);
test.resources.setSrcDirs([]);
main.java.srcDir("$rootDir/collect/src/main/java")
main.resources.srcDir("$rootDir/collect/src/main/resources")
test.java.srcDir("$rootDir/collect/src/test/java")
test.resources.srcDir("$rootDir/collect/src/test/resources")

dependencies {
    api project(path: ":inject-main")
    testImplementation "junit:junit:4.13"
    api project(path: ":collect-api")
}

// Setup publishing to coordinates(net.wetheinter): net.wetheinter:xapi-collect
// GenEnd net.wti.gradle.settings.plugin.XapiSettingsPlugin
// Done generating buildfile for :collect at file://$rootDir/collect/src/main/collectMain.gradle
