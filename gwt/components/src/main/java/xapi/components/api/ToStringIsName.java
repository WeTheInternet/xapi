package xapi.components.api;

/**
 * A marker interface to denote that a type's name comes from the toString() method,
 * rather than the name() method, as is commonly used for enum types.
 *
 * @author James X Nelson
 *
 */
public interface ToStringIsName {}
