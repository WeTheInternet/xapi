<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>net.wetheinter</groupId>
    <artifactId>xapi-gwt-parent</artifactId>
    <version>0.3-SNAPSHOT</version>
  </parent>

  <!-- 
  Standalone gwt module; runs all gwt tests, and builds uber jar.
  
  Deferring tests until the dist module allows all gwt source jars to compile
  quickly, but ensures that any module dependent on a working distribution
  gets a working jar.
   -->
  <artifactId>xapi-gwt</artifactId>
  <packaging>jar</packaging>
  <name>XApi - Gwt uber jar</name>
  <url>WeTheInter.net</url>
  <description>
    This module exists solely to package all other gwt modules into a single
    uber jar.  This makes deploying to non-mavenized targets much easier.
    
    To use the uber jar in maven projects, you much inherit using a classifier:
    groupId: wetheinter.net
    artifactId: xapi-gwt
    classifier: uber
    
    Of course, you would be wise to inherit your dependencies individually;
    the uber jar is intended for projects like collide,
    which have complex configuration, and adding many jars would be a pain.
 </description>
  <properties>
  </properties>

  <dependencies>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-inject</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-reflect</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-process</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-collect</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-model</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-io</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.wetheinter</groupId>
      <artifactId>xapi-gwt-test</artifactId>
      <scope>test</scope>
    </dependency>
    
    <dependency>
      <groupId>com.google.gwt</groupId>
      <artifactId>gwt-dev</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.google.gwt</groupId>
      <artifactId>gwt-user</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>javax.validation</groupId>
      <artifactId>validation-api</artifactId>
      <version>1.0.0.GA</version>
      <classifier>sources</classifier>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <!-- Builds our uber-jar of "everything a gwt compile needs -->
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>uberjar</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <finalName>${project.artifactId}-${project.version}</finalName>
              <appendAssemblyId>false</appendAssemblyId>
              <archive /><!-- TODO put in a useful main class -->
              <!-- <manifest> <mainClass>my.package.to.my.MainClass</mainClass> </manifest> -->
              <descriptors>
                <descriptor>src/main/assembly/jar-assembly.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
        </executions>
      </plugin>
       <plugin>
        <!-- Runs all gwt tests before building uber jar. -->
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <additionalClasspathElements>
            <additionalClasspathElement>${project.build.sourceDirectory}</additionalClasspathElement>
            <additionalClasspathElement>${project.build.testSourceDirectory}</additionalClasspathElement>
          </additionalClasspathElements>
          <useManifestOnlyJar>false</useManifestOnlyJar>
          <forkMode>always</forkMode>
          <systemProperties>
            <property>
              <name>gwt.args</name>
              <value>-ea -userAgents gecko1_8 -out ${project.build.directory}/www-test</value>
            </property>
          </systemProperties>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
