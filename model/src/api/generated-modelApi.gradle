import org.gradle.api.tasks.SourceSet;

ext.xapiModern = 'true'
// GenStart XapiSettingsPlugin

apply from: "$rootDir/gradle/xapi-modern.gradle"
String repo = project.findProperty("xapi.mvn.repo")
if (repo) {
  repositories {
    maven {
      name = 'xapiLocal'
      url = repo
    }
  }
}
String javaPlugin = findProperty('xapi.java.plugin') ?: 'java-library'
apply plugin: javaPlugin
repositories.mavenCentral()

SourceSet main = sourceSets.maybeCreate('main');

main.java.setSrcDirs([]);
main.resources.setSrcDirs([]);
main.java.srcDir("$rootDir/model/src/api/java")
main.resources.srcDir("$rootDir/model/src/api/resources")

dependencies {
  implementation project(path: ":collect-api")
  implementation project(path: ":inject-main")
}
// GenEnd XapiSettingsPlugin
// Done generating buildfile for :model at file://$rootDir/model/src/api/generated-modelApi.gradle
